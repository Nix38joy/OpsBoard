export function IncidentCreatePage() {
  return (
    <div className="page">
      <h1>Create Incident</h1>
      <p>
        Form module placeholder. We will implement React Hook Form + zod validation in the next
        feature step.
      </p>
      <section className="card">
        <p>Fields planned: title, description, severity, priority, team, assignee, SLA due date.</p>
      </section>
    </div>
  );
}
